{
  imports = [
    # when you make a module you'll put it here as the relative path
    # for example a hyprland module could be './hyprland.nix'
    # you can also import directories if you wanna sort your modules, which looks like './applications' as an example
    # if you import a directory the directory also needs a default.nix like this
  ];
}
